# 05 路线图与风险

## 里程碑

### M0 — 新仓库脚手架与内容快照（2 周）

新产品为独立仓库，不依赖、不改动原仓库；内容与代码以一次性快照拷入。

- 新仓库 monorepo 骨架（interview 代码作模板拷入，从零提交，不带 Git 历史）
- 内容快照拷入 `packages/content/`（排除构建产物与死代码，清单见 04-migration）
- 图片拷贝期处理：SVGO 压缩 + 跨仓库去重，统一入 `packages/content/assets/`（已定方案一：Git 直存 + 打进 docs 镜像）
- content-kit 雏形：frontmatter schema + 校验器 + 统一 ID 分配；frontmatter 批量补齐
- leetgpu 编号错位修正、各仓库统计口径改自动统计
- 知识点受控词表 v1（合并三方标签体系）
- **验收**：新仓库内全部内容 frontmatter 校验通过；任意内容可用统一 ID 引用；原仓库零改动

### M1 — 内容融合（3–4 周）

- 拷贝期去重执行完毕（同源内容只保留一份，交叉引用改为统一 ID 链接）
- `apps/docs` 三分区 VitePress 站上线（leetcode/leetgpu 主题组件拷入；ai-infra-notes 自研站模板用 VitePress 重写，小样验证后全量）
- 内容 → DB 同步管线 + 全站静态搜索索引
- 产题管线（SKILL.md + cannbot 脚本）拷入并验证外部依赖可用；原仓库内容线停更（2026-09 决策），周赛等后续更新只在新仓库生产
- **验收**：单一 URL 空间下可浏览全部学习与题库内容；CI 构建全绿（lint + 分区构建无 OOM）

### M2 — 应用融合（4 周）

- web 应用新增 Learn 路径页、Problems 题库页、Dashboard（进度 + 掌握度雷达）
- 账号体系落地（OAuth + 邮箱登录），进度/面试/评测数据按用户隔离——对外产品的前置
- 内容页进度标记、题单功能（hot-interview / 配套练习）
- 算法题评测沙箱（Docker 隔离，C++/Python/SQL）替换本机裸跑
- 面试报告闭环：薄弱点 → 学习章节/练习题推荐
- **验收**：一条完整闭环可走通——按路径学习某 Day → 做配套题并 AC → 面试被追问该考点 → 报告推荐回流

### M3 — 对外发布（4–6 周）

对外产品定位（2026-09 决策）下，以下为必需项而非可选项：

- GPU 题评测引流产品化：leetgpu.com 跳转引导 + `.cu` harness 下载 + 刷题状态手动标记/回传（不自建 GPU 沙箱——2026-09 决策：部署机无 GPU）
- 配额与限流体系：评测提交、LLM 面试的计量与限流能力建设（2026-09 决策：上线初期免费用户不限额，限额默认无限、后续配置开启）；LLM 成本控制（模型分级 + 结果缓存 + 用量打点）
- 监控告警：评测队列积压、LLM 成本与延迟、构建时长、错误率
- 自有机器 docker-compose 单机部署上线（自有域名 + Caddy 自动 TLS）；SEO 冷启动（sitemap、结构化数据）；与原三站零耦合
- 上线节奏：开放注册制（邮箱注册，2026-09 决策）；配套注册验证码限流、防批量注册，上线后重点观察 LLM 成本与评测负载
- **验收**：真实外部用户走通完整闭环；GPU 题引流评测链路可用；成本与故障异常有告警

### M4 — 持续增长（持续迭代）

- 内容扩充：论文精读补完（9/27 → 全量）、题库持续跟进（SKILL.md 管线 + LLM 抽题、周赛自动跟进）
- 商业化：免费/付费分层落地（配额差异化，如无限次 LLM 面试、评测配额提升为付费项）
- 社区功能（评论/讨论、题解共建）——视留存数据决定是否投入
- 增强项（有 GPU 资源才启动）：自建 GPU 评测沙箱，届时收编 `leetgpu-challenges`（题库元数据 + `challenge.py` 用例生成）作为基座

## 风险清单

| 风险 | 等级 | 缓解 |
|---|---|---|
| 评测安全（裸跑用户代码） | 高 | M2 容器沙箱为产品化硬门槛，此前不对外开放注册 |
| docs 构建规模失控（4100+ 页已 OOM） | 高 | 三分区独立构建；设定触发线（单区 >15min / >8GB 内存）提前评估换框架 |
| ai-infra-notes 自研站 → VitePress 重写工作量被低估 | 中 | M1 先做 week1 小样验证再全量；交互功能优先用现成插件 |
| 知识点标注质量（4000+ 题靠 LLM 标注） | 中 | 规则优先 + 人工抽查高频题；标注错误只影响推荐精度，不阻断主流程 |
| 双端组件重复（Vue/React） | 中 | 职责切分：docs 只读、web 交互；不为 docs 开发任何交互组件 |
| 内容口径继续漂移 | 低 | 构建期 lint（含陈旧表述检测）失败即中止，替代人肉维护 |
| LLM 依赖与成本（面试官、抽题、标注） | 中 | 多用户后面试调用是主要变动成本：按用户配额 + 模型分级 + 缓存；题库抽取为离线批处理 |
| 滥用与刷接口（开放注册制下的必然问题） | 中 | 注册验证码 + IP 限流防批量注册；配额限流中间件 + 评测队列并发上限 + 异常用量告警 |
| 单机自托管的可用性与带宽天花板 | 中 | 监控告警 + compose 自动重启 + MySQL 定时备份；内容在 Git、数据在 MySQL，整体可搬迁——流量超单机承载时迁移云主机不留架构障碍 |
| GPU 评测依赖外部平台 | 低 | leetgpu.com 引流为长期方案；其不可用只影响 GPU 题评测，学习/算法评测/面试链路不受影响 |

## 开放问题（需决策）

1. **商业化模式**：订阅 vs 免费+配额的分层边界。已定部分（2026-09 决策）：上线初期免费用户**不限额**，但配额计量与限流功能照常建设（限额值默认无限，后续仅改配置即可开启分层）；付费分层设计待 M4 前决策。
2. **产品命名与域名**：AIInfra Lab 为暂定名。
3. **与原仓库的引流关系**：原站点停更但不下线，继续运行不受影响。是否要在原三站加"内容已迁移至新产品"的公告引流（这是唯一需要触碰原仓库的动作）？默认不做，待新产品稳定后再议。

已决策（留档）：

- ~~冷启动范围~~ → 开放注册制，邮箱注册（2026-09）；配套注册验证码限流与防批量注册。
- ~~图片存储~~ → Git 直存 + 打进 docs 镜像起步，宿主机 assets 卷挂载兜底（2026-09，详见 04-migration 主要风险节）。
- ~~GPU 评测沙箱~~ → 不自建：部署机无 GPU，GPU 题评测引流 leetgpu.com（2026-09）；有 GPU 资源时作为 M4 增强项重启。
- ~~内容更新地~~ → 原仓库停更，后续更新只在新仓库（2026-09，详见 04-migration 边界约束）。
