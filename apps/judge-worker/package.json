{
  "name": "@ailab/judge-worker",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "start": "tsx src/index.ts",
    "dev": "tsx watch src/index.ts",
    "test": "pnpm --filter @ailab/judge-core build && vitest run"
  },
  "dependencies": {
    "@ailab/contracts": "workspace:*",
    "@ailab/judge-core": "workspace:*",
    "mysql2": "^3.11.5",
    "tsx": "^4.19.2"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
