{
  "name": "ai-infra-lab",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "db:generate": "pnpm --filter @ailab/server db:generate",
    "db:migrate": "pnpm --filter @ailab/server db:migrate"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@ailab/cli": "link:apps/cli"
  }
}
